// write a code for squares of the elements of the array.
#include <iostream>
using namespace std;
void square(int arr[],int n){
    int prod=1;
    for (int i = 0; i < n; i++)
    {
        
    }
    
}
int main(){
    int arr[10];
    int size;
    cout<<"Enter the size ";
    cin>>size;
    for (int i = 0; i <size; i++)
    {
        cin>>arr[i];
    }
}